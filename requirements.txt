# ── Web app (runtime) ─────────────────────────────────────────
flask>=3.0
pypdf>=4.0
gunicorn>=21.0
python-dotenv>=1.0

# ── IR Engine ─────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# ── Semantic retrieval (CPU-only torch to keep image small) ───
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
sentence-transformers>=2.2
faiss-cpu>=1.7

# ── Ground truth generation (optional - needs OPENAI_API_KEY) ─
openai>=1.0

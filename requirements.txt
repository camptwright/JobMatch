# ── Web app (runtime) ─────────────────────────────────────────
flask>=3.0
pypdf>=4.0
gunicorn>=21.0
python-dotenv>=1.0

# ── IR Engine ─────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# ── Semantic retrieval ────────────────────────────────────────
sentence-transformers>=2.2
torch>=2.0
faiss-cpu>=1.7

# ── Ground truth generation (optional — needs OPENAI_API_KEY) ─
openai>=1.0

# ── EDA figures (optional — only needed for scripts/generate_figures.py) ─
matplotlib>=3.7

# ── Checkpoint PDF generation (optional) ──────────────────────
reportlab>=4.0
